server:
    port: 8080

spring:
    application:
        name: "typress"

logging:
    config: "classpath:logback-spring.xml"
    level:
        root: info
    pattern: "[%d{yyyy-MM-dd'T'HH:mm:ss.SSS}][%level][%X{trace_id}][%C:%line]:%m%n"
    home: "/data/logs/"
