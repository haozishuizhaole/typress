server:
    port: 8080

spring:
    application:
        name: "typress"
    datasource:
        driver-class-name: org.sqlite.JDBC
        url: jdbc:sqlite:/data/typress/database/typress.db
    mvc:
        throw-exception-if-no-handler-found: true
    web:
      resources:
          add-mappings: false

logging:
    config: "classpath:logback-spring.xml"
    level:
        root: info
    pattern: "[%d{yyyy-MM-dd'T'HH:mm:ss.SSS}][%level][%X{trace_id}][%C:%line]:%m%n"
    home: "/data/logs/"

# Typress系统配置
typress: