server:
    port: 8080

spring:
    application:
        name: "typress"
    profiles:
        active: "sqlite"
    #    datasource:
    #        driver-class-name: org.sqlite.JDBC
    #        url: jdbc:sqlite:/data/sqlite/database/typress.db
    #        username: root
    #        password: 123456
#    mvc:
#        dispatch-options-request: true

logging:
    config: "classpath:logback-spring.xml"
    level:
        root: info
    pattern: "[%d{yyyy-MM-dd'T'HH:mm:ss.SSS}][%level][%X{trace_id}][%C:%line]:%m%n"
    home: "${typress.data-space}/logs"

mybatis:
    mapper-locations: "classpath:/mybatis/mapper/*.xml,classpath:/mybatis/mapper/*/*.xml"
    #    mapper-base-packages: "cc.chenzhihao.typress.core.infrastructure.persistence.sqlite.mapper"

################# Typress系统配置 #################
typress:
    data-root-dir: ""
    data-space: "${typress.data-root-dir}/typress"
#    persistence:
#        type: "sqlite"

# Web配置
#web:
#    # 跨域配置
#    cors:
#        mappings:
#            -   "mappingPattern": "/**"
#                # "allowedOrigins": "*"
#                "allowedOriginPatterns": "*"
#                "allowedMethods": "*"
#                "allowedHeaders": "*"
#                "exposedHeaders": "Authorization"
#                "allowCredentials": true
#                "maxAge": 3600
#    # 权限配置
#    permission:
#        path-patterns:
#            - "*"
#        exclude-path-patterns:
#            - "*"
